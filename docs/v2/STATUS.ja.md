# V2 ステータス

> この日本語版は [`STATUS.md`](STATUS.md) の翻訳です。**英語版を canonical（正典）とし、解釈に差がある場合は英語版を優先します。**

2026-09-22 時点の状況:

- **Release sequencing:** `v0.8.0 — Backend Portability & Recovery Safety` が released baseline です。live control/capability/registry/Hub-Agent schema は 12/8/8/6 のまま、durable execution-safety は v15、Agent M1 persistence は v6 のままです。v0.8 admitted scope は #377 authoritative Hub pre-enqueue non-delivery settlement、#379 structured semantic-refusal remediation、#380 explicit constrained execution-environment/PATH semantics、#222 second-real-backend portability evidence です。#139 signed-token dogfood / #217 recovery parity / #228 physical Linux FIDO2 は separate withheld support-claim gate のまま、#215 は unsupported です。
- **Desktop semantic path:** complete / accepted。same-context の native element click/type/key targeting と、real-Cua による background AX element-action evidence を含みます。
- **Browser core semantic path:** prepare、bind、inspect、navigate、click、type、dialog、pointer semantics まで complete / accepted です。
- **Browser transfer:** complete / accepted。upload/download は scoped CUMG ref と Agent-private の bounded staging を使用し、任意の host path を northbound に公開しません。private staging を確立できない場合も privacy を保ったまま、Agent startup が失敗した正確な bounded stage / I/O class を host-local に記録します。
- **Optional first-class Human Handoff:** CUMG では bounded Window / Terminal-PTY coordination を accepted 済みです。Window は upstream `WindowHandoffAdapter`、Terminal は upstream `TerminalHandoffAdapter` を使い、PTY/process containment と content-free verification は CUMG に残します。canonical Handoff runtime/checkpoint と Human surface は操作対象 Agent が所有し、Hub は CUMG authorization、ledger、quarantine、conservative pre-dispatch fence を保持します。runtime/transport loss 時は Handoff を迂回せず fail closed します。upstream Window #85 には upstream 自身の first-class same-LAN direct physical rerun が残りますが、CUMG #152 は closed のままです。
- **Current execution-safety boundary:** durable execution-safety schema は **v15** です。effectful work の結果が曖昧なら `Indeterminate` として device を quarantine し、automatic retry / replay は行いません。schema v6 は text/input delivery が適用されたが commit されていない状態を、本当の no-effect resolution と区別します。schema v7 は payload-free / privacy-preserving な text-input evidence envelope と optional keyed candidate matching を追加します。schema v8 は closed recovery-evidence read lane を追加し、明示 allowlist 済みの non-mutating evidence read だけを quarantine 中にも許可します。generic shell/process とすべての mutation/activation/write capability は引き続き拒否されます。schema v9 は caller-retained status-only effectful response-loss recovery、schema v10 は historical `Indeterminate` を変更せず reviewed low-impact ambiguity を継続点として受け入れる local-user-presence `current_state_accepted` retirement disposition を追加します。schema v11 は permanent exact replay tombstone と64件 bounded detailed retirement history を分離し、compact tombstone index は独立して4096件上限・exhaustion 時 fail closed とします。schema v12 はprivacy-bounded semantic-authorization admission evidence（snapshot revision/digest + constraint kind/rule ID）を追加します。schema v13 は acknowledged-unknown PointerClick current-state acceptance の explicit mutation-resume barrier/record を追加し、fresh generation の Human re-arm が完了するまで新しい mutation を拒否し、v13 state を失う downgrade を reject します。 schema v14 は新規application launch/termination ambiguity向けのbounded private recovery-target identityを追加し、historical v13 stateはreadableのまま、target-bearing v14 stateのlossy downgradeは拒否します。 schema v15 は、command encoding がenqueue前に失敗した場合、またはbounded Hub→Agent mpscがframeをacceptせずrejectした場合というexactで狭いケースだけにHub-authoritative pre-enqueue non-delivery evidenceを追加します。Hubはterminal `cancelled` / confirmed-not-executed recordをlive state解放前にpersistします。successful enqueue後のfailureは引き続きambiguityとして`Indeterminate` / quarantine pathへ進みます。v15 evidenceはreplayを一切authorizeせず、後続のeffectful attemptにはfresh operation IDが必要です。
- **Operator inspection / reconciliation:** `v2_maint inspect-quarantine` は read-only で、raw payload / owner identity を公開せず、以前から device を block している正確な `blocking_operation_id` と bounded recovery metadata を返します。`v2_maint audit-reconciliation` は最新の Hub/Agent durable state を相関し、authoritative terminal evidence、legacy/non-authoritative marker、observational correlation を明示的に区別します。どちらも quarantine を clear せず replay authority にもなりません。`compare-quarantine-request` は correlation-only で、返せるのは `same_request`、`different_request`、`unavailable` だけです。
- **Operator-ready incident brief (#233):** `v2_maint incident-brief` は exact #133 reconciliation audit に bounded quarantine metadata と optional closed-schema diagnostic observation を合成します。external finding は `observational_only` / `unavailable` に強制され、authority を名乗ることも `supported_decisions` を広げることもできません。矛盾は CUMG evidence を上書きせず明示され、shared mutation owner/epoch も available/unavailable/not-requested として read-only に表示できます。JSON / compact text output は raw request/result/desktop/log payload を含みません。
- **Authoritative self-reconciliation / retirement:** execution-safety schema v4 は exact effectful dispatch fence を persist し、fresh authenticated generation 上の exact signed Agent terminal proof が揃う場合だけ self-resolve できます。v5 は permanently unknowable な legacy ambiguity のうち狭い allowlist だけを対象に、exact operation ID を permanent non-replayable のまま維持する別の offline retirement path を追加します。automatic settlement、manual resolution、retirement は意図的に別状態で、いずれも persistence-gated です。
- **Authoritative backend execution receipt (#290):** effectful backend は、exact operation/device generation/capability revision/capability/dispatch grant/provider identity と必要な #289 target に bind する review 済み payload-free durable receipt contract を opt-in できます。exact receipt は Agent M1 state schema **v6** に persist し、既存 `AgentTerminalEvidence` に変換して signed #124 self-reconciliation path をそのまま使います。Hub execution-safety はreleased v0.8でv15、live control/capability/Hub-Agent schemaは変更しません。historical Agent state schema v5 は receipt 無しなら readable、receipt-bearing v6 state の lossy downgrade は拒否します。default adapter と現在の Cua MCP adapter には review 済み durable receipt lookup がないため、Agent が definite result を得る前の Cua response loss は引き続き `Indeterminate` / operator-required で、log/current-state heuristic は observational-only のままです。
- **Verified single-Mac backup/restore (#347):** packaged macOS workflow は reviewed writer 停止 + shared mutation-authority lock 保持時だけ coherent snapshot を作成し、exact CUMG/Handoff runtime identity、durable Hub/Agent state、reviewed in-root trust/secret、mutation owner/epoch、quarantine/replay barrier を保持します。browser/ephemeral/runtime staging、lock、log、socket、cache、stale recovery-key copy は除外します。canonical manifest は restore 時に backup 外で保持した SHA-256 を必須とします。restore は non-running staged phase と explicit activation を分離し、exact clean profile path だけへ戻します。settlement を捏造せず mutation authority を変更せず snapshot freshness も主張しません。activation 後の quarantine 変化は exact persisted #290 receipt 等、既存 authoritative recovery/reconciliation path だけに限定されます。
- **Offline recovery compatibility:** authority-bearing `v2_maint resolve` は Hub の完全停止を要求し、authoritative checkpoint の既存 durable writer contract を維持します。maintenance binary は publication 前に representability を検証し、deployed Hub と version-paired artifact として install する必要があります。新しい checkout の maintenance binary を古い deployed Hub 所有 state に対して任意に使う運用は support しません。Issue #100 は complete です。trusted physical macOS の Secure Enclave/user-presence acceptance で実 ambiguous desktop operation を replay せず resolve し、Hub restart 後も durable resolution が authoritative のまま維持されることを確認しました。
- **Constrained process/shell environment contract (#380, v0.8.0):** `execute_process` / `shell` はchild environmentをclearし、Agent service environmentのallowlisted subsetだけをinheritします。`PATH`はservice environmentに存在する場合だけinheritし、login / interactive profileは読み込みません。caller `env.PATH` overrideは引き続きdenyし、service `PATH`外のexecutableはreview済みabsolute pathを使います。tool schemaはraw PATH/environment valueを公開せず `x-cumg-execution-environment` でbounded policyだけをmachine-readableに示し、discovery / authorization authorityは追加しません。
- **Structured semantic-refusal remediation (#379, v0.8.0):** 既存stable refusal codeをcanonicalのまま維持し、既知のbrowser / interaction / capability / session refusalにだけboundedな `required_actor`、`next_action`、`same_operation_replay_safe=false`、`remediation_is_authority=false`、`fresh_call_required` metadataをadditiveに返します。mappingはprovider-neutralで、unknown codeからremediationを推測せず、hintはimplicit foreground switch、browser-route fallback、scope expansion、consent、replayを一切authorizeしません。`mutation_resume_required` はrequired actorをlocal userとして明示し、`device_indeterminate` は既存のより強いreconciliation guidanceを維持します。

- **Second real-backend portability (#222, v0.8.0):** macos-mcp 0.4.0が `ListApplications`、`MovePointer`、`PointerClick` のnarrowな `ComputerUseBackendAdapter` sliceを実装しました。trusted-Mac physical acceptanceでreal stdio `Snapshot` -> Finder Dock observation -> real `Move` -> real `Click` が成功しています。専用Hub testではstale generation/revisionがdispatch前に拒否され、dispatch済みでprovider outcomeを証明できない場合はdurable `Indeterminate` / `BackendOutcomeUnproven` quarantineとなり、restart後もauto-resolution/replayなしで維持されることを証明しています。PR #385はprotected Rust / passthrough / CodeQL / release-candidate bundle / Linux・macOS・Windows Cua smokeがすべてgreenでmerge済みです。[`acceptance/V2_SECOND_BACKEND_PORTABILITY_ACCEPTANCE.md`](acceptance/V2_SECOND_BACKEND_PORTABILITY_ACCEPTANCE.md) を参照してください。
- **Privacy-safe northbound failures / actionable indeterminate UX (#115):** released v0.5 baseline は **control v10 / capability v6**、#106 の historical v0.6 development pairing は **control v11 / capability v7**、released v0.6 は **control v12 / capability v8** です。working-directory denial、timeout、program/environment policy、spawn failure、cancellation、`agent_offline`、`device_indeterminate` など expected execution-policy/runtime failure を bounded client-visible category として維持しつつ、raw path、command、environment value、device identity、provider text、OS error string は公開しません。`device_indeterminate` は execution が既に起きた可能性、blind replay が unsafe であること、exact `get_operation` + reconciliation を優先すること、後続 effectful work には fresh operation ID が必要なことも明示します。command-text heuristic や replay authority は追加しません。unknown internal failure は引き続き generic に fail closed します。
- **Host reliability / diagnostics:** `v2_doctor` は restart-safety state を変更せず、authoritative に証明できる in-band diagnostic self-observation と本当の blocking quarantine を区別します。Browser staging startup failure は bounded local stage/I/O diagnostic を出します。controlled `StorageFull` injection により、Agent checkpoint persistence の容量枯渇が Agent を fail-closed exit させ、remote では `agent_offline` として見えることを確認済みです。失敗前の committed checkpoint / replay barrier は authoritative のまま維持され、容量復旧後は通常の service-manager reconnect で回復します。`v2_doctor` は state/temp filesystem の容量について coarse / read-only signal だけを公開します。
- **Effectful response-loss recovery (#103):** すべての effectful process/shell/Desktop/Browser tool は caller-retained stable `operation_id` を受け取り、Hub-local `get_operation` は Agent liveness や replay に依存せず exact owner と current exact-capability authorization を再確認します。process/shell は bounded output を保持し、それ以外の effectful Desktop/Browser operation は authoritative state/receipt と payload-free `effectful_status` marker だけを persist します。bounded recovery archive は最大8件 / encoded total 256 KiB のままで、unknown/evicted reference が original operation を retry-safe にすることはありません。この marker を含む schema v9 checkpoint の v8 downgrade は拒否します。
- **Local-user online quarantine recovery:** explicit recovery-key provisioning の背後で実装済みです。Agent device key は recovery authority ではなく、fresh Hub-signed challenge は separately pinned P-256 endpoint recovery key だけで解決できます。初期 macOS signer は user-presence access control 付き Secure Enclave key を使用します。automated protocol/persistence coverage と trusted physical acceptance は完了し、local user presence 後にのみ authorization が publish され、ambiguous operation は durably resolve、restart 後も resolution を維持し、旧 operation は replay されませんでした。
- **Lane-scoped operator readiness (#226):** `v2_doctor` は existing authenticated state / capability / quarantine authority から導出する privacy-bounded readiness summary を追加します。control-plane、Computer Use observation、filesystem observation、generic effectful execution、browser-effectful execution を区別し、durable quarantine がある場合は independently healthy な observation lane を隠さず effectful lane だけを `indeterminate_fenced` と表示します。evidence 欠落・不整合は green と推測せず `unavailable` / `unknown` にします。この summary は diagnostic-only で、resolve / retry / replay / dispatch / authority grant はできません。
- **Least-privilege filesystem observation root (#104):** Agent の process/shell cwd root と `ReadFile`/`ListDirectory` root を別設定にします。file root はより狭くでき、canonicalization / symlink-escape denial を維持し、cwd root へのfallbackは行いません。file root 未設定/空設定は startup fail closed とし、packaged service example と upgrade runbook も explicit migration を含みます。
- **Ranged/deterministic filesystem observation (#105):** [`V2_WORKSPACE_FILESYSTEM.ja.md`](V2_WORKSPACE_FILESYSTEM.ja.md) は stateless 8 KiB file range、deterministic bounded directory cursor、明示 scan/result budget、capability-rooted reopen semantics を定義します。#314 で control schema 10 / capability schema 6 に統合し、mixed version は fail closed を維持します。
- **Effectful execution budget (#319):** [`V2_EXECUTION_BUDGET.ja.md`](V2_EXECUTION_BUDGET.ja.md) は duration-bearing Cua command が bounded deadline に収まらない場合 dispatch 前に reject します。packaged Cua timeout は 30 秒、effective execution budget は 24 秒です。pre-dispatch rejection は terminal/retry-safe、実際の post-dispatch timeout は `Indeterminate`/no-replay のままです。#314 で signed timeout ack を Hub-Agent schema 6 と readiness/release metadata に統合しました。
- **Bounded atomic workspace mutation (#107):** [`V2_WORKSPACE_MUTATION.ja.md`](V2_WORKSPACE_MUTATION.ja.md) は explicit writable root と deny-wins exclusion を持つ exact `WriteWorkspaceFile` authority を定義します。#314 で explicit opt-in とし、packaged default は disabled、enabled は explicit write root 必須、cwd/read root から write authority への fallback は禁止、quarantine 中 readiness は `indeterminate_fenced` です。
- **Retrievable truncated process/shell output (#83):** [`V2_EPHEMERAL_DATA_REFS.ja.md`](V2_EPHEMERAL_DATA_REFS.ja.md) は通常の1 stream 16 KiB inline resultを変えず、exact `ReadProcessOutput` authorityの背後でboundedなAgent-private retained bytesを扱います。#314でexplicit private ephemeral parentをpackageし、retained bytesはnon-authoritative / rollback対象外のまま、live refをpersistせずexact runtime/release schema identityを記録します。
- **v0.5 release integration (#314):** release-manifest schema **v3** と installed runtime-manifest schema **v4** は exact Hub-Agent/control/capability schema identity を記録します。`v2_doctor` は nested schema のどれか1つでも mismatch なら `runtime_manifest / invalid_schema_or_identity` で fail closed します。generic macOS / single-Mac / Linux / Windows profile は explicit ephemeral parent と mutation disabled default を持ちます。v0.4 -> v0.5 migration で cwd/read root から write root を導出せず、ephemeral bytes を authoritative rollback state に昇格させません。Linux/Windows packaging/CI evidence は新規 physical Cua smoke の claim ではありません。
- **Durable single-Mac maintenance status (#234):** reviewed source-build upgrade は owner-private な atomic transaction record に exact CUMG/Handoff source identity、durable phase、bounded terminal class、target runtime generation、rollback identity、mutation-authority owner/epoch、explicit completion gate を記録します。`v2_maint upgrade-status` は read-only で、maintenance resume/retry や quarantine/authority 変更は行いません。source build は default Cargo jobs=2、transaction/service mutation 前に free-space floor 6144 MiB 未満を拒否し、その後の Cargo ENOSPC は install 前の `build_storage_exhausted` として分類します。
- **Durable online-recovery confirmation (#109):** Agent はpending authorizationに対してexactなHub署名 `RecoveryResolved` ACKを検証し、challenge/authorization handoff fileをclearする前にowner-private receiptをatomic保存します。fresh challenge受信時は旧receiptをclearします。`v2_recover resolve --wait-secs` / `v2_recover confirm` はexact request/device/generation/operation/decisionを検証した後だけdurable completionを返し、local authorization publicationだけをterminal successとは扱いません。
- **Unified operator status (#235):** `v2_status` を normal first single-Mac diagnostic とします。JSON schema v1 は既存 #226 readiness、quarantine/recovery、Handoff status、mutation authority、verified runtime identity、#234 maintenance state を bounded な `overall` / `primary_reason` / `next_action` に合成し、別の authority/state machine は作りません。human renderer も同じ schema から生成します。Handoff locator/ID、device/principal identity、payload、credential、grant、fingerprint は出力しません。
- **Guided quarantine recovery (#236 / #255 / #323):** `v2_recover guide` は authoritative #233 `supported_decisions` を変更せず、そのsetが空の場合だけ Human-only historical assertion lane を別に表示します。plan schema v3 は Human lane を interactive-only / automatic-selection禁止のまま維持し、Human review を challenge-scoped にします。challenge expiry または exact binding/generation rollover では visible prompt と stale selection context を無効化し、fresh signed review と fresh Human choice を必須にし、user-presence authentication が開始されなかった場合も明示します。Human assertion は既存 `confirmed_completed` / `confirmed_not_executed` を inconclusive audit assessment のまま再利用し、exact challenge/incident再検証、Secure Enclave user presence、#109 durable ACK、no replay、exact quarantine clear、既存 #235 `v2_status` post-check を維持します。`--json` は引き続き read-only planning 専用です。
- **Artifact-backed Product Readiness path (#237):** macOS single-Mac release profile は CUMG/Handoff runtime source checkout 不要で、closed manifest で exact CUMG/Handoff commit を bind し、local-user recovery tooling を含み、activation 前 verification、separately provisioned trust/secrets を使う clean first install、既存 one-shot durable upgrade / paired rollback contract を提供します。install success には installed `v2_doctor` / `v2_status` の両方が healthy であることが必要です。Linux/Windows は引き続き reviewed candidate evidence であり official binary-installer claim はしません。
- **Local-Human current-state acceptance (#137):** 完了。`v2_recover accept-current-state` は reviewed `Scroll` / `MovePointer` ambiguity だけに使える独立した authority-bearing macOS path です。separately provisioned Secure Enclave recovery key と fresh signed challenge を再利用し、strictly newer な authenticated/durable generation を要求します。durable audit は `execution_outcome=indeterminate`、`operational_disposition=current_state_accepted`、`authority=local_user_presence`、`replayed=false` を保持し、解除するのは quarantine だけです。exact old operation ID は permanent tombstone のままで、terminal receipt や completed/not-executed claim は作りません。northbound MCP tool でもありません。Shell/process/filesystem と allowlist 外 capability は fail closed します。
- **Replay tombstone/history separation (#136):** 完了。詳細 retirement audit は64件で rotation し、別の lossless compact exact-ID tombstone index が restart/migration をまたぐ permanent no-replay を維持します。rotation した audit に対応する full retired operation detail も compact します。index は4096件上限で bounded capacity health を公開し、exhaustion 時は quarantine を維持して追加 retirement を拒否します。v5-v10 state は exact migrate し、detail rotation 後の downgrade は拒否します。
- **Recovery & Reconciliation sequencing（2026-09-19更新）:** #103/#137/#136 と #256 / PR #258 は完了済みです。#227 / PR #252 は automated greenで、2026-09-19にcancel -> approve -> durable exact resolution -> restart/no-replayまでphysical Windows acceptance PASS、#228 / PR #259 は implementation/CI complete・physical Linux acceptance pending です。#253/#254/#115 は完了済みで、#255 Human historical-resolution guidance も完了済みです。released safety/recovery invariant failure の新 evidence が出た場合だけ blocker に昇格します。
- **Installed runtime/operator compatibility (#253):** `v2_doctor` / `v2_status` は recovery 前に exact manifest/runtime pairing、operator tool の stale/unavailable、authoritative checkpoint reader compatibility を分類します。old-tool/new-runtime 混在は skew/stale として fail closed、reader より新しい checkpoint schema は直接 incompatible と表示し、identity 不明も action-required のままです。`v2_status` はこれらを `inspect_upgrade` へ案内し、diagnostic 自体は read-only で recovery state を mutation probe しません。
- **macOS recovery-key readiness (#254):** `v2_doctor` / `v2_status` はincident前にlocal Secure Enclave sealed representation、exact manifest-verified helper、Hub recovery verifierが実際にpairされているかを表示します。probeはhelperのnon-interactive `public` operationだけを使い、LocalAuthentication/signingは起動せず、boundedなready/unprovisioned/missing/mismatch/helper/unknown stateだけを返します。partial/mismatch provisioningはexplicit `provision_recovery`へ案内し、diagnosticがrecovery authorityをcreate/rotate/replaceすることはありません。
- **Hosted Hub support gate (#215):** [`V2_CLOUD_RUN_HUB.ja.md`](V2_CLOUD_RUN_HUB.ja.md) は現時点の Cloud Run support を NO-GO とします。support claim 前に external durable authoritative state、monotonic writer fencing、stale-writer dispatch denial、platform request timeout より前の proactive Agent stream rotation、fail-closed hosted shutdown、Agent/northbound authentication boundary を分離した one-port h2c routing、rollout concurrency acceptance、physical Agent + real Cua evidence を必須とします。
- **Durable hosted Hub state core (#283 candidate):** provider-neutral `HubAuthoritativeStateStore` seamを実装し、monotonic revision / writer epoch、compare-and-commit、exact read-after-commit、後方互換local `CheckpointStore` adapter、deterministic conformance/failure injectionを持たせています。automated evidenceでold live Agent streamを保持するstale writerのdispatch拒否、conditional raceのexact one-winner、provider unavailable時のno-dispatch、durable dispatch後replacementの`Indeterminate` quarantine復元、stale recovery拒否、current-schema fence欠落拒否、legacy/local maintenance互換を確認します。Cloud Run/database固有providerはまだsupport claimせず、hosted backup/revision/physical acceptanceは#284/#215に残します。
- **Hosted one-port ingress (#282 candidate):** PR #285はmerge済みで、production hosted-only `PORT` / h2c compositionを `v2_hub` に実装しています。Agent gRPC、protected MCP、hosted Handoff OAuth resourceをexact fail-closed classifier配下の1 listenerに同居させ、hosted modeではHub TLS file、loopback `CUMG_V2_MCP_BIND`、trusted-proxy auth、同一MCP/Handoff resource identity、同一OIDC audienceを拒否します。real h2c integration testでTonic gRPC + MCP/Handoff HTTP/2が同一listenerを通り、cross-surface content typeとunknown pathが拒否されることを確認済みです。VM/single-host listener behaviorは変更しません。#283の残るhosted-provider evidence、#353/#284、#215の残りphysical gate完了までCloud Run supportはNO-GOです。
- **Hosted Handoff composition (#275/#277):** [`V2_HOSTED_HANDOFF_TOPOLOGY.ja.md`](V2_HOSTED_HANDOFF_TOPOLOGY.ja.md) で CUMG operation/quarantine/replay authority は hosted Hub、canonical physical Handoff mutation authority/checkpoint は Agent、hosted routing は non-authoritative のまま維持します。PR #281 は merge済みで、closed principal/device/action operator-control core、separate OAuth HTTP resource、principal/action-bound opaque surface-context handle、locator minimization、restart時に空となるprocess-memory-only provider-blind route/viewer/transport generation fenceが `main` にあります。reviewed Handoff dependency #276/v0.4.5 もcomplete。hosted/physical acceptanceは #282/#283/#353/#284 に残ります。
- **Reviewed Handoff v0.4.5 consumer update (#276):** PR #280でcomplete。exact packaged Handoff pinはrelease commit `523b547ff10696487095956ca0b8c3db628819c4` / package `0.4.5` で、release packagingはexact checkout commit + package versionを検証し、canonical Handoff authorityはAgent-ownedのままです。#277/#282/#283/#353/#284 acceptance完了までhosted supportはwithholdします。
- **Unix/Linux process containment (#96 / #267):** [`V2_UNIX_PROCESS_CONTAINMENT.ja.md`](V2_UNIX_PROCESS_CONTAINMENT.ja.md) のportable process-group claimは変更しません。[`V2_LINUX_CGROUP_V2_CONTAINMENT.ja.md`](V2_LINUX_CGROUP_V2_CONTAINMENT.ja.md) で#267のLinux限定opt-in backendを追加し、explicit dedicated delegation、pre-exec placement、root-level `cgroup.kill`、recursive `populated 0` proof、startup/termination fail-closedを定義します。macOS/portable-Unix claimは不変です。
- **Multi-principal signed-token identity (#139):** provider-neutral OIDC/JWT verification は PR #269 で `main` へ merge 済みです。既存 `AccessTokenVerifier -> AuthenticatedClientPrincipal` seam を再利用し、asymmetric algorithm allowlist と pinned/bounded JWKS cache/refresh の下で exact issuer/audience/time/subject を検証し、exact device/capability authorization は変更しません。automated test で2つのsigned subjectが既存authorizer上で別principalのまま扱われることを確認します。physical signed-token dogfood acceptance は未完了のため #139 はOPENのままとし、provider-specific support claim はまだ行いません。
- **Typed semantic authorization (#221):** `0.4.0` candidate changeでimplementation completeです。exact principal/device/capability authorizationをmandatoryのまま維持し、optional typed constraintがfinalized `TypeText` UTF-8 byteとnormalized `BrowserNavigate` requested originをnarrowします。exact `(revision, digest)` snapshotはHub runtimeでimmutable、allow operationはbounded rule identityだけをpersistし、stale snapshotはdispatch前cancelで`Indeterminate`を作りません。両constrained fieldのprovider materialization preservationもtest済みです。
- **Product Readiness closeout (#213):** 完了。cross-cutting release gate は恒久 [`../PRODUCT_READINESS.ja.md`](../PRODUCT_READINESS.ja.md) checklist が引き継ぎ、これまで散在していた backup/restore operator boundary は [`V2_BACKUP_RESTORE.ja.md`](V2_BACKUP_RESTORE.ja.md) で明文化しました。#104 filesystem-root separation と #111 reproducible performance benchmark は完了済みです。#215 Cloud Run Hub support-gate design は完了済みですが、durable-state/writer fencing、hosted secret rotation、hosted/physical acceptance が未完了のため Cloud Run は unsupported のままです。one-port ingress candidate は #282 で実装済みです。#96 調査は完了済みで、Linux-specific stronger containment は managed execution 配下の #267 で継続します。
- **V1 compatibility:** V1 は regression/reference と既存 deployment 向けに引き続き利用できます。V2 作業中も V1 regression/conformance coverage は必須です。残る #14/#15 は upstream Cua に blocked された observation として扱い、active CUMG release blocker にはしません。

## 有効な契約

- [`V2_080_RELEASE_SCOPE.ja.md`](V2_080_RELEASE_SCOPE.ja.md) — released `0.8.0` Backend Portability & Recovery Safety scope / durable-state migration・rollback / acceptance / support-claim boundary。
- [`V2_070_RELEASE_SCOPE.ja.md`](V2_070_RELEASE_SCOPE.ja.md) — released `0.7.0` Operator Ergonomics & Recovery Evidence scope / durable-state migration・rollback / acceptance / support-claim matrix。
- [`V2_060_RELEASE_SCOPE.ja.md`](V2_060_RELEASE_SCOPE.ja.md) — released `0.6.0` Managed Developer Execution scope / compatibility / acceptance / support-claim matrix。
- [`V2_040_RELEASE_SCOPE.ja.md`](V2_040_RELEASE_SCOPE.ja.md) — released `0.4.0` scope / acceptance record / support-claim matrix。
- [`V2_SEMANTIC_AUTHORIZATION.ja.md`](V2_SEMANTIC_AUTHORIZATION.ja.md) — typed narrow-only final-command semantic authorization / durable snapshot binding。
- [`V2_POSITIONING.ja.md`](V2_POSITIONING.ja.md) — canonical product boundary の日本語版。
- [`V2_P0_EXECUTION_SAFETY.md`](V2_P0_EXECUTION_SAFETY.md) — uncertainty-aware execution と no-auto-replay invariant。
- [`V2_OPERATION_RECOVERY.ja.md`](V2_OPERATION_RECOVERY.ja.md) — northbound response loss 後の durable / bounded process・shell result recovery。
- [`V2_BACKUP_RESTORE.ja.md`](V2_BACKUP_RESTORE.ja.md) — exact paired runtime state と unresolved quarantine を保持する coherent single-Mac backup/restore。
- [`V2_INTERACTION_CONTEXT.md`](V2_INTERACTION_CONTEXT.md) — scoped interaction state と backend-reference ownership。
- [`V2_GUI_SEMANTIC_CAPABILITIES.ja.md`](V2_GUI_SEMANTIC_CAPABILITIES.ja.md) — Desktop semantic surface。
- [`V2_BROWSER_SEMANTIC_CAPABILITIES.md`](V2_BROWSER_SEMANTIC_CAPABILITIES.md) — Browser semantic surface と transfer boundary。
- [`V2_CUA_PARITY_MATRIX.ja.md`](V2_CUA_PARITY_MATRIX.ja.md) — Cua compatibility/parity classification。
- [`V2_THREAT_MODEL.ja.md`](V2_THREAT_MODEL.ja.md) — security claim と non-claim。
- [`V2_STANDARDIZATION.md`](V2_STANDARDIZATION.md) と [`V2_P2_REPLACEMENT_SEAMS.md`](V2_P2_REPLACEMENT_SEAMS.md) — maintained OSS / standards の replacement boundary。

## Acceptance evidence

- [`acceptance/V2_BROWSER_CORE_ACCEPTANCE.md`](acceptance/V2_BROWSER_CORE_ACCEPTANCE.md) — Browser core closeout。
- [`acceptance/V2_BROWSER_TRANSFER_ACCEPTANCE.md`](acceptance/V2_BROWSER_TRANSFER_ACCEPTANCE.md) — Browser transfer contract、threat control、automated coverage、trusted-Mac real-Cua evidence。
- [`acceptance/V2_LOCAL_DESKTOP_ACCEPTANCE.md`](acceptance/V2_LOCAL_DESKTOP_ACCEPTANCE.md) — trusted physical Desktop acceptance の procedure/evidence。
- [`acceptance/V2_M1_ACCEPTANCE.md`](acceptance/V2_M1_ACCEPTANCE.md) — 以前の secure-Agent milestone acceptance を evidence として保持。
- [Issue #100](https://github.com/git-ksk/computer-use-mcp-gateway/issues/100) / merged PR #101 — trusted physical macOS Secure Enclave/user-presence acceptance が pass。Hub restart 後も durable resolution を維持し、ambiguous operation の replay なしを確認済み。

## 履歴記録

初期 prototype と progress record は [`../archive/v2/`](../archive/v2/) に archive されています。設計の provenance を保持するための記録であり、現在の実行可能な setup instruction ではありません。

- **Managed developer jobs (#106, v0.6.0):** [V2_MANAGED_JOBS.ja.md](V2_MANAGED_JOBS.ja.md) は独立した exact `ManagedJobControl` / `ManagedJobObserve` authority、bounded lease/hard lifetime/output、Hub-owned opaque ref、process-group/Job Object terminal proof、非同期 termination ambiguity に対する durable Agent fail-closed recovery を定義します。#106 は historical v0.6 development pairing として control schema 11 / capability schema 7 を導入し、released v0.6 は control schema 12 / capability schema 8 です。Hub-Agent envelope schema は6のままです。

- **Sandboxed Playwright/E2E (#114, v0.6.0):** [V2_PLAYWRIGHT_SANDBOX.ja.md](V2_PLAYWRIGHT_SANDBOX.ja.md) は独立した exact PlaywrightTestControl / PlaywrightTestObserve authority と operator-configured の replaceable Docker/Podman-compatible provider を定義します。complete config、digest-pinned image inspect、owner-scoped startup orphan recovery がすべて成功するまで capability を advertise しません。fixed profile は network none、approved workspace の read-only bind、Agent-private writable artifact bind、sanitized environment、resource ceiling、fixed Playwright executable/reporter/output contract を使います。terminal success には host runtime client の終了ではなく provider container absence の証明が必要で、cleanup を証明できない場合は既存の no-replay / Indeterminate safety path に入ります。public ref は独立した pwtest_ namespace を使います。current live control/capability schema は 12/8、persisted registry は8、Hub-Agent envelope は6のままです。
